#include<stdio.h>

int main() {

    /*here are integer
    int num = 786;
    printf("Enter you num: %d \n", num);
    */


    /*
    here are flooting 
    float number = 12.34;
    printf("Enter your Float Number: %f \n", number);
    */


    /* here are character
    char star = '*';
    printf("Enter your star: %c \n", star);
    */


    /* // NOW we see input output
    int age;
    printf("Enter your number: ");
    scanf("%d", & age);
    printf("Hmara number ho jaiga : %d", age);
    */

    /* let's write a basic program
    int a, b; 
    printf("Enter number of a: ");
    scanf("%d",&a);   // we use ampersand only in scanf function

    printf("Enter number of b: ");
    scanf("%d",&b);  // we use ampersand only in scanf function

    // int sum = a + b; 
    printf("total of sum: %d", a + b);
    */

    // int side;
    // printf("Enter num of side: ");
    // scanf("%d",&side);

    // printf("area of square: %d", side * side );


    /* Area of square
    float side;
    printf("Enter number of side: ");
    scanf("%f", & side);

    printf("Area of square is: %f", side * side);
    */

    // Area of circle 

    float radius;
    printf("Enter number of radius: ");
    scanf("%f", &radius);

    printf("Area of circle: %f", 3.14 * radius * radius); 
    return 0;
}